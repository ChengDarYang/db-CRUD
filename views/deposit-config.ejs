<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deposit Configuration - Transaction Balance App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .config-form {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
        }
        .current-settings {
            background-color: #e9ecef;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Deposit Configuration</h1>
            <a href="/" class="btn btn-outline-primary">Back to Transactions</a>
        </div>

        <% if (settings) { %>
            <div class="current-settings">
                <h3>Current Settings</h3>
                <div class="row">
                    <div class="col-md-4">
                        <p><strong>Interval:</strong> <%= settings.interval_days %> days</p>
                    </div>
                    <div class="col-md-4">
                        <p><strong>Amount:</strong> $<%= settings.amount %></p>
                    </div>
                    <div class="col-md-4">
                        <p><strong>Start Date:</strong> <%= new Date(settings.start_date).toLocaleDateString() %></p>
                    </div>
                </div>
                <form action="/update-deposits" method="POST" class="mt-3">
                    <button type="submit" class="btn btn-primary">Update Deposits to Current Date</button>
                </form>
            </div>
        <% } %>

        <div class="config-form">
            <h3>Configure New Deposit Settings</h3>
            <form action="/deposit-config" method="POST">
                <div class="row g-3">
                    <div class="col-md-4">
                        <label for="interval_days" class="form-label">Deposit Interval (days)</label>
                        <input type="number" class="form-control" id="interval_days" name="interval_days" 
                               min="1" required>
                    </div>
                    <div class="col-md-4">
                        <label for="amount" class="form-label">Deposit Amount</label>
                        <input type="number" step="0.01" class="form-control" id="amount" name="amount" 
                               min="0.01" required>
                    </div>
                    <div class="col-md-4">
                        <label for="start_date" class="form-label">Start Date</label>
                        <input type="date" class="form-control" id="start_date" name="start_date" required>
                    </div>
                </div>
                <div class="mt-3">
                    <button type="submit" class="btn btn-primary">Save Settings</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 